/* East Window Studio — interaction, fixed-height planar placement, history and local storage. */
'use strict';
(()=>{
const $=s=>document.querySelector(s),$$=s=>Array.from(document.querySelectorAll(s));
const paths={homeleaf:'M4 20V10l8-7 8 7v11H9v-8m0 3c0-8 10-8 10-8 0 8-5 10-10 8',home:'M3 10l9-7 9 7M5 9v12h5v-7h4v7h5V9',leaf:'M5 20C2 9 7 3 21 3c0 13-6 18-16 17Zm0 0L16 9m-7 7v-5m4 1h5',sun:'M12 8a4 4 0 1 0 0 8 4 4 0 0 0 0-8ZM12 2v2m0 16v2M2 12h2m16 0h2M5 5l1 1m12 12 1 1M5 19l1-1M18 6l1-1',sunrise:'M3 18h18M6 15a6 6 0 0 1 12 0M12 2v4M3 8l3 2m15-2-3 2M2 22h20',moon:'M20 15A9 9 0 0 1 9 3 9 9 0 1 0 20 15Z',camera:'M3 7h4l2-3h6l2 3h4v14H3Zm9 3a4 4 0 1 0 0 8 4 4 0 0 0 0-8Z',save:'M4 3h13l4 4v14H3V3ZM7 3v6h9V3M7 21v-8h10v8',help:'M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18ZM9 9c0-4 6-4 6 0 0 2-3 2-3 5m0 3v.1',info:'M12 3a9 9 0 1 0 0 18 9 9 0 0 0 0-18ZM12 11v6m0-10v.1',ruler:'M3 17 17 3l4 4L7 21ZM9 12l3 3m0-6 3 3m0-6 3 3',move:'M12 2v20M2 12h20M9 5l3-3 3 3M9 19l3 3 3-3M5 9l-3 3 3 3m14-6 3 3-3 3',orbit:'M8 4C1 8 2 18 9 20m-3-1 3 1-1-4M16 4c7 4 6 14-1 16m1-13V4h3M8 12a4 4 0 1 0 8 0 4 4 0 0 0-8 0Z',plus:'M12 5v14M5 12h14',minus:'M5 12h14',cube:'m12 2 10 5v10l-10 5-10-5V7ZM2 7l10 5 10-5M12 12v10M7 4.5l10 5',plan:'M3 3h18v18H3ZM3 9h8V3m0 6v5h10M9 21v-7H3',undo:'M9 5 3 11l6 6M3 11h12c8 0 7 10 0 10',redo:'M15 5l6 6-6 6m6-6H9c-8 0-7 10 0 10',sliders:'M4 5h16M4 12h16M4 19h16M8 3v4m8 3v4m-5 3v4',hand:'M7 12V5a2 2 0 0 1 4 0v8-9a2 2 0 0 1 4 0v9-6a2 2 0 0 1 4 0v6-3a2 2 0 0 1 3 1v5c0 4-3 6-7 6-3 0-5-2-7-4l-4-5c-1-2 1-3 2-2l3 3',lock:'M6 10h12v11H6ZM8 10V6a4 4 0 0 1 8 0v4m-4 5v2',close:'m6 6 12 12M6 18 18 6',check:'m5 12 4 4L20 5',eye:'M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12Zm10-3a3 3 0 1 0 0 6 3 3 0 0 0 0-6Z',eyeoff:'M3 3l18 18M9 5c7-2 13 7 13 7l-4 4M6 7l-4 5s5 9 13 6',rotate:'M4 9V3m0 6h6M4 9a8 8 0 1 1-1 6',sofa:'M5 11V7c0-2 2-3 7-3s7 1 7 3v4M3 10h3v5h12v-5h3v10H3Zm2 10v2m14-2v2',monitor:'M3 3h18v14H3ZM12 17v4m-5 0h10',grid:'M3 3h7v7H3ZM14 3h7v7h-7ZM3 14h7v7H3ZM14 14h7v7h-7Z',flower:'M12 8c-5-10-11 0-4 3-11 0-7 11 0 6-2 10 10 7 6 0 8 7 13-3 4-5 9-5 0-12-3-4ZM12 11a3 3 0 1 0 0 6 3 3 0 0 0 0-6Z',copy:'M8 8h13v13H8ZM4 16H3V3h13v1',trash:'M3 6h18M9 6V3h6v3M5 6l1 15h12l1-15M10 10v7m4-7v7',light:'M8 15a7 7 0 1 1 8 0l-1 3H9Zm1 6h6',download:'M12 3v12m-5-5 5 5 5-5M4 16v5h16v-5',arrow:'M5 12h14m-5-5 5 5-5 5'};
function icon(n){return `<svg class="icon" viewBox="0 0 24 24" aria-hidden="true"><path d="${paths[n]||paths.leaf}"/></svg>`;}
function icons(root=document){root.querySelectorAll('[data-icon]').forEach(e=>e.innerHTML=icon(e.dataset.icon));}
icons();const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let renderer,items=[],arch=[],selected=null,presetId='original',tab='layouts',filter='all',query='',mode='move',labels=false,closedCurtains=false,undo=[],redo=[],frameQueued=false,storageOK=true,saveTimer,toastTimer,drag=null,pointers=new Map(),pinch=null,space=false;
const KEY='east-window-studio.v1',SLOTS='east-window-studio.slots.v1',R=NestRoom.room,PI=Math.PI;
const baseById=new Map(NestRoom.base.map(o=>[o.id,o]));
const copyBase=()=>NestRoom.base.map(o=>({...o}));
function toast(msg){$('#toast').textContent=msg;$('#toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('show'),2600);}
function snapshot(){return {version:1,preset:presetId,curtains:closedCurtains,objects:items.map(({id,x,z,r,visible,parent,surface,species,name})=>({id,x,z,r,visible,parent:parent||null,surface:surface||null,species,name}))};}
function checkpoint(){undo.push(snapshot());if(undo.length>50)undo.shift();redo=[];updateHistory();}
function updateHistory(){$('#undoBtn').disabled=!undo.length;$('#redoBtn').disabled=!redo.length;}
function scheduleSave(){clearTimeout(saveTimer);$('#saveStatus').innerHTML='<i></i> 正在保存…';saveTimer=setTimeout(()=>{try{localStorage.setItem(KEY,JSON.stringify(snapshot()));storageOK=true;$('#saveStatus').innerHTML='<i></i> 已保存到此浏览器';}catch(e){storageOK=false;$('#saveStatus').textContent='保存不可用 · 请导出布局';}},400);}
function world(o){return Nest3D.transform(renderer.matrix(o,renderer.map),[0,0,0]);}
function parentDims(o){const p=renderer.map.get(o.parent);return p?[p.w,p.d]:null;}
function limits(o){let c=Math.abs(Math.cos(o.r||0)),s=Math.abs(Math.sin(o.r||0)),hx=(o.w*c+o.d*s)/2,hz=(o.w*s+o.d*c)/2;
 if(o.parent){const p=renderer.map.get(o.parent);return {xmin:-p.w/2+hx+.015,xmax:p.w/2-hx-.015,zmin:-p.d/2+hz+.01,zmax:p.d/2-hz-.01};}
 if(o.surface==='bay')return{xmin:3.06+o.w/2,xmax:3.54-o.w/2,zmin:-2.5+o.d/2,zmax:2.5-o.d/2};
 if(o.wallMount)return{xmin:-R.w/2+hx+.04,xmax:R.w/2-hx-.04,zmin:-2.84,zmax:-2.84};
 return{xmin:-R.w/2+hx+.04,xmax:R.w/2-hx-.04,zmin:-R.d/2+hz+.04,zmax:R.d/2-hz-.04};}
function clamp(o){const l=limits(o);o.x=Math.max(l.xmin,Math.min(l.xmax,o.x));o.z=Math.max(l.zmin,Math.min(l.zmax,o.z));if(l.xmax<l.xmin)o.x=(l.xmin+l.xmax)/2;if(l.zmax<l.zmin)o.z=(l.zmin+l.zmax)/2;}
const supportY={sofa:.55,coffee:.45,desk:.81,side:.73,sideboard:.685,pouf:.43};
function restore(data,fromImport=false){if(!data||data.version!==1||!Array.isArray(data.objects)||data.objects.length>120)throw Error('这不是有效的东窗小筑布局文件。');let next=copyBase(),seen=new Set();const map=new Map(next.map(o=>[o.id,o]));
 for(const s of data.objects){if(!s||typeof s.id!=='string'||seen.has(s.id)||![s.x,s.z,s.r].every(v=>typeof v==='number'&&Number.isFinite(v)&&Math.abs(v)<1000))throw Error('布局包含无效或重复的物品坐标。');seen.add(s.id);let o=map.get(s.id);if(!o){if(!/^extra-[a-zA-Z0-9-]{1,40}$/.test(s.id)||!NestRoom.plants[s.species])throw Error('布局包含未知物品。');o={id:s.id,name:'新增植物',type:'plant',category:'plants',w:.36,d:.36,x:0,z:0,r:0,y:0,visible:true,variant:7};next.push(o);map.set(o.id,o);}o.x=s.x;o.z=s.z;o.r=s.r%(PI*2);o.visible=s.visible!==false;if(s.name&&typeof s.name==='string')o.name=s.name.slice(0,60);if(o.type==='plant'&&NestRoom.plants[s.species])o.species=s.species;
 const original=baseById.get(o.id),movable=o.type==='plant'||['tea','tissue','books','keyboard','laptop','console'].includes(o.type);if(movable){o.parent=s.parent&&Object.hasOwn(supportY,s.parent)?s.parent:null;o.surface=!o.parent&&s.surface==='bay'?'bay':null;o.y=o.parent?supportY[o.parent]:o.surface==='bay'?.74:0;}else{o.parent=original?.parent;o.y=original?.y||0;}
 }
 for(const o of items)renderer.dispose(o.parts);items=next;items.forEach((o,i)=>{o.pickId=i+1;o.parts=renderer.upload(NestRoom.model(o));});presetId=NestRoom.presets.some(p=>p.id===data.preset)?data.preset:'custom';closedCurtains=!!data.curtains;buildArch();renderer.setScene(arch,items);items.forEach(clamp);selected=selected&&renderer.map.get(selected.id)||null;refresh();if(fromImport)toast('布局已导入，原操作仍可撤销。');}
function buildArch(){for(const a of arch)renderer.dispose(a.parts);arch=NestRoom.architecture(closedCurtains).map(o=>({...o,parts:renderer.upload(o.parts)}));renderer.setScene(arch,items);}
function applyPreset(id){const p=NestRoom.presets.find(p=>p.id===id);if(!p)return;checkpoint();const objects=NestRoom.base.map(o=>{const c=p.changes[o.id];return {...o,...(c?{x:c[0],z:c[1],r:c[2]}:{})};});restore({version:1,preset:id,curtains:false,objects});scheduleSave();toast(`已切换「${p.name}」，可以继续拖动微调。`);}
function select(o){selected=o;$('#inspector').classList.toggle('open',!!o);if(o?.ceiling){renderer.ceiling=true;}if(o?.wallMount){renderer.walls=true;}renderInspector();if(tab!=='layouts')renderSidebar();draw(false);}
function changed(){presetId='custom';$('#currentLayout').textContent='我的自由布置';scheduleSave();draw();renderInspector();if(tab!=='layouts')renderSidebar();else $$('.preset-card').forEach(c=>c.classList.remove('active'));}
function draw(shadow=true){if(frameQueued)return;frameQueued=true;requestAnimationFrame(()=>{frameQueued=false;if(!renderer)return;updateRing();renderer.frame(shadow);updateLabels();});}
function updateRing(){const old=arch.find(o=>o.id==='selection-ring');if(old){renderer.dispose(old.parts);arch=arch.filter(o=>o!==old);}if(selected&&selected.visible){const b=new Nest3D.Builder(),w=selected.w+.10,d=selected.d+.10,c='#88a76f',ll=.18;for(const x of[-w/2,w/2])for(const z of[-d/2,d/2]){b.box(ll,.009,.018,c,x-Math.sign(x)*ll/2,.017,z,.003);b.box(.018,.009,ll,c,x,.017,z-Math.sign(z)*ll/2,.003);}arch.push({id:'selection-ring',model:renderer.matrix(selected,renderer.map),helper:true,parts:renderer.upload(b.finish())});}renderer.setScene(arch,items);}
function updateLabels(){const e=$('#selectionLabel');if(selected&&selected.visible&&(!selected.ceiling||renderer.ceiling)){let p=world(selected),h=selected.type==='plant'?NestRoom.plants[selected.species]?.height||.7:selected.type==='sofa'?1.05:selected.type==='monitor'?1.1:selected.type==='desk'?.84:.75;const q=renderer.project([p[0],p[1]+h+.10,p[2]]);e.hidden=q[0]<5||q[0]>renderer.width-5||q[1]<15||q[1]>renderer.height-30;if(!e.hidden){e.style.left=q[0]+'px';e.style.top=q[1]+'px';$('#selectionName').textContent=selected.name;}}else e.hidden=true;
 if(labels){$('#plantLabels').innerHTML=items.filter(o=>o.category==='plants'&&o.visible&&o!==selected&&(!o.parent||renderer.map.get(o.parent)?.visible)).map(o=>{let p=world(o),q=renderer.project([p[0],p[1]+(NestRoom.plants[o.species]?.height||.5),p[2]]);if(q[0]<0||q[0]>renderer.width||q[1]<0||q[1]>renderer.height)return'';return `<span style="left:${q[0]}px;top:${q[1]}px">${esc(o.name)}</span>`;}).join('');}else $('#plantLabels').innerHTML='';
 const east=renderer.project([3.6,0,0]),center=renderer.project([0,0,0]),angle=Math.atan2(east[1]-center[1],east[0]-center[0])*180/PI+90;$('#compassArrow').setAttribute('transform',`rotate(${angle} 36 36)`);
}
function preview(type){let leaves='';const kind=type||'unknown';if(kind==='sofa')return `<svg viewBox="0 0 180 125" aria-hidden="true"><ellipse cx="90" cy="105" rx="62" ry="8" fill="#d7ddc8"/><rect x="25" y="47" width="130" height="45" rx="9" fill="#b9c2a2"/><rect x="31" y="67" width="118" height="26" rx="7" fill="#d2d4b7"/><rect x="21" y="59" width="15" height="37" rx="6" fill="#bdc6a5"/><rect x="144" y="59" width="15" height="37" rx="6" fill="#bdc6a5"/>${[43,69,96,122].map((x,i)=>`<rect x="${x}" y="52" width="22" height="25" rx="3" fill="${['#698663','#d2a782','#809575','#c99479'][i]}" transform="rotate(${i%2?5:-5} ${x} 52)"/>`).join('')}<path d="M31 95v8m118-8v8" stroke="#7f8068" stroke-width="4"/></svg>`;
 if(!NestRoom.plants[kind])return `<svg viewBox="0 0 180 125" aria-hidden="true"><ellipse cx="90" cy="105" rx="49" ry="7" fill="#d7ddc8"/><path d="M45 67 88 44l51 26-44 25Z" fill="#f7ecd7"/><path d="m45 67 50 28v10L45 78Z" fill="#bca27f"/><path d="m95 95 44-25v10l-44 25Z" fill="#a89373"/><path d="M53 82v21m77-16v18" stroke="#7b8068" stroke-width="4"/></svg>`;
 let n=kind==='fern'?15:kind==='bird'?7:9;for(let i=0;i<n;i++){let a=(i/n-.5)*2.9,x=90+Math.sin(a)*(kind==='bird'?39:49),y=kind==='bird'?20+Math.abs(a)*23:43+Math.abs(a)*20;leaves+=`<path d="M90 104Q${90+Math.sin(a)*15} 66 ${x} ${y}" fill="none" stroke="#6d8b59" stroke-width="1.6"/><ellipse cx="${x}" cy="${y}" rx="${kind==='fern'?5:kind==='bird'?10:13}" ry="${kind==='fern'?19:kind==='bird'?28:21}" fill="${['#76965c','#416d45','#96ac70','#5a8551'][i%4]}" transform="rotate(${-a*39} ${x} ${y})"/><path d="m${x} ${y+14} 0 -27" stroke="#b7c393" stroke-width=".7" transform="rotate(${-a*39} ${x} ${y})"/>`;}
 if(kind==='hydrangea'||kind==='flowers'){leaves='';for(let i=0;i<7;i++){let x=59+i*10,y=40+Math.sin(i*1.2)*12;leaves+=`<path d="M90 101  ${x} ${y}" stroke="#839965" stroke-width="1.5"/>`;for(let j=0;j<7;j++)leaves+=`<circle cx="${x+Math.sin(j*2.4)*10}" cy="${y+Math.cos(j*2.4)*10}" r="7" fill="${kind==='flowers'?['#dda15a','#d88857'][j%2]:['#b6b0ce','#969ec0','#d0cadd'][j%3]}"/>`;}}
 return `<svg viewBox="0 0 180 140" aria-hidden="true"><ellipse cx="91" cy="132" rx="33" ry="5" fill="#d8ddc8"/>${leaves}<path d="M69 99h43l-7 31H76Z" fill="${kind==='flowers'?'#c6ad68':'#e0d4ba'}"/><ellipse cx="90.5" cy="100" rx="22" ry="5" fill="#c9bda2"/><ellipse cx="90" cy="99" rx="18" ry="3" fill="#8c8b66"/><path d="m77 104 4 22" stroke="#f6edd9" stroke-width="3" opacity=".6"/></svg>`;
}
function miniPlan(p){let map=new Map(NestRoom.base.map(o=>[o.id,{...o}]));for(const[id,pos]of Object.entries(p.changes))Object.assign(map.get(id),{x:pos[0],z:pos[1],r:pos[2]});let els='';for(const id of['rug','desk','sideboard','sofa','coffee','side','pouf','chair1','chair2']){const o=map.get(id),x=112+o.x*16,y=48+o.z*11,w=o.w*16,h=o.d*11;els+=`<rect x="${x-w/2}" y="${y-h/2}" width="${w}" height="${h}" rx="${id==='pouf'?h/2:2}" transform="rotate(${-o.r*180/PI} ${x} ${y})" fill="${({rug:'#d7d7c5',sofa:'#aab79a',coffee:'#f8f2df',desk:'#cbb494',pouf:'#d8b562'})[id]||'#c6c9b0'}"/>`;}
 for(const id of['bird','monstera','fern','hydrangea','dracaena']){const o=map.get(id);els+=`<circle cx="${112+o.x*16}" cy="${48+o.z*11}" r="${id==='bird'?7:5}" fill="${id==='hydrangea'?'#a5a9c0':'#78966c'}"/><circle cx="${110+o.x*16}" cy="${46+o.z*11}" r="3" fill="#aec29a"/>`;}
 return `<svg viewBox="0 0 224 96" aria-hidden="true"><rect x="51" y="12" width="123" height="73" rx="4" fill="#e3ddc9"/><rect x="54" y="15" width="117" height="67" fill="#f4ecd8"/><path d="M54 82V15h117v67" stroke="#c3b698" stroke-width="2" fill="none"/><path d="M172 20v55" stroke="#91aaa0" stroke-width="3"/>${els}<path d="m182 45 8 0m-3-3 3 3-3 3" stroke="#c6b591" fill="none"/></svg>`;}
function renderSidebar(){const el=$('#sideContent');if(tab==='layouts'){el.innerHTML=NestRoom.presets.map(p=>`<article class="preset-card ${presetId===p.id?'active':''}" data-preset="${p.id}" role="button" tabindex="0" aria-label="应用${p.name}布局"><div class="preset-art">${miniPlan(p)}<span class="preset-index">${p.eyebrow.split(' / ')[0]}</span><span class="preset-badge">${p.tag}</span></div><div class="preset-body"><div class="preset-title">${p.name}${presetId===p.id?`<span class="check">${icon('check')}</span>`:`<span style="color:#b5bba4">${icon('arrow')}</span>`}</div><p>${p.desc}</p></div></article>`).join('');el.querySelectorAll('[data-preset]').forEach(e=>{e.onclick=()=>applyPreset(e.dataset.preset);e.onkeydown=v=>{if(v.key==='Enter'||v.key===' '){v.preventDefault();e.click();}};});}
 else if(tab==='objects'){el.innerHTML=`<input class="search-input" id="objectSearch" type="search" placeholder="找一件家具，一盆绿植…" value="${esc(query)}" aria-label="搜索物品"><div class="filter-row">${[['all','全部'],['furniture','家具'],['plants','植物'],['decor','软装'],['electronics','电器'],['lighting','吊灯']].map(([k,n])=>`<button class="filter ${filter===k?'active':''}" data-filter="${k}">${n}</button>`).join('')}</div><div id="objectList"></div>`;const input=$('#objectSearch');input.oninput=()=>{query=input.value;renderObjectList();};el.querySelectorAll('[data-filter]').forEach(e=>e.onclick=()=>{filter=e.dataset.filter;renderSidebar();});renderObjectList();}
 else{el.innerHTML=`<p class="section-tip">${items.filter(o=>o.category==='plants').length} 个植物 / 花材对象。识别基于外形；小盆与花材状态保留不确定性。点选可定位与校正。</p>${items.filter(o=>o.category==='plants').map(o=>{const p=NestRoom.plants[o.species];return `<div class="plant-card" data-plant="${o.id}" role="button" tabindex="0">${preview(o.species)}<div><h3>${esc(o.name)}</h3><small>${p.confidence} · ${p.kind}</small><p>${p.name}</p></div></div>`;}).join('')}`;el.querySelectorAll('[data-plant]').forEach(e=>{e.onclick=()=>{select(renderer.map.get(e.dataset.plant));$('#sidebar').classList.remove('open');};e.onkeydown=v=>{if(v.key==='Enter')e.click();};});}}
function renderObjectList(){const list=$('#objectList');if(!list)return;const arr=items.filter(o=>(filter==='all'||o.category===filter)&&o.name.includes(query));list.innerHTML=arr.length?arr.map(o=>`<div class="object-list-item ${selected?.id===o.id?'active':''}" data-object="${o.id}" role="button" tabindex="0"><span class="list-symbol">${icon(o.category==='plants'?'leaf':o.category==='electronics'?'monitor':o.category==='lighting'?'light':o.category==='furniture'?'sofa':'grid')}</span><div><strong>${esc(o.name)}</strong><small>${o.parent?'随承载家具移动':o.surface==='bay'?'飘窗平面':o.ceiling?'固定吊装高度':'地面投影可调整'}</small></div><button data-hide="${o.id}" aria-label="${o.visible?'隐藏':'显示'}${esc(o.name)}">${icon(o.visible?'eye':'eyeoff')}</button></div>`).join(''):'<div class="empty-list">没有找到这件物品。</div>';list.querySelectorAll('[data-object]').forEach(e=>{e.onclick=()=>{select(renderer.map.get(e.dataset.object));$('#sidebar').classList.remove('open');};e.onkeydown=v=>{if(v.key==='Enter')e.click();};});list.querySelectorAll('[data-hide]').forEach(e=>e.onclick=v=>{v.stopPropagation();checkpoint();const o=renderer.map.get(e.dataset.hide);o.visible=!o.visible;changed();});}
function overlap(o){if(o.parent||o.category==='decor'||o.wallMount||o.ceiling||o.surface==='bay')return[];const op=world(o),axes=[[Math.cos(o.r),-Math.sin(o.r)],[Math.sin(o.r),Math.cos(o.r)]];
 function corners(a){let pos=world(a),c=Math.cos(a.r),s=Math.sin(a.r);return[-1,1].flatMap(i=>[-1,1].map(j=>[pos[0]+i*a.w/2*c+j*a.d/2*s,pos[2]-i*a.w/2*s+j*a.d/2*c]));}
 const A=corners(o);return items.filter(b=>{if(b===o||!b.visible||b.parent||b.category==='decor'||b.wallMount||b.ceiling||b.surface==='bay')return false;const bp=world(b);if(Math.hypot(op[0]-bp[0],op[2]-bp[2])>4)return false;const B=corners(b),ax=[...axes,[Math.cos(b.r),-Math.sin(b.r)],[Math.sin(b.r),Math.cos(b.r)]];return ax.every(v=>{const aa=A.map(p=>p[0]*v[0]+p[1]*v[1]),bb=B.map(p=>p[0]*v[0]+p[1]*v[1]);return Math.min(Math.max(...aa),Math.max(...bb))-Math.max(Math.min(...aa),Math.min(...bb))>.045;});});}
function renderInspector(){const el=$('#inspectorContent');if(!selected){el.innerHTML=`<div class="empty-inspector"><span class="big-leaf">${icon('leaf')}</span><h3>让生活，<br>慢慢长出绿意。</h3><p>点选房间里的家具或植物，<br>为它找到一个更喜欢的位置。</p><div class="tip-card"><strong>小小布置提示</strong><p>${NestRoom.presets.find(p=>p.id===presetId)?.tip||'按住物品拖动，只在水平面调整。桌上物件会跟着桌子一起移动。'}</p></div></div>`;return;}
 const o=selected,p=o.category==='plants'?NestRoom.plants[o.species]:null,wp=world(o),distance=Math.max(0,3.55-wp[0]),collision=overlap(o);const portable=o.type==='plant'||['tea','tissue','books','keyboard','laptop','console'].includes(o.type);
 el.innerHTML=`<div class="object-preview"><span class="photo-tag">${p?'BOTANICAL NOTES':'A PIECE OF YOUR HOME'}</span>${preview(p?o.species:o.type)}</div><h2 class="object-heading">${esc(o.name)}</h2><p class="object-latin">${p?esc(p.latin):'Photo-inspired object · 可独立布置'}</p><div class="chips"><span class="chip">${p?p.kind:o.w.toFixed(2)+' × '+o.d.toFixed(2)+' m · 估算'}</span>${p?`<span class="chip uncertain">${p.confidence}</span>`:''}</div>${p?`<div class="care-card"><div class="field-title">${icon('sun')}距东窗约 ${distance.toFixed(1)} m</div><p>${p.light}</p><small>距离为模型估算，不等于实际光照强度。</small></div>`:''}<div class="inspector-section"><div class="field-title">${icon('move')}平面位置 <span style="margin-left:auto;font-size:8px;font-weight:400">${o.parent?'相对承载面':'相对房间中心'}</span></div><div class="position-inputs"><label>X <input id="posX" type="number" value="${o.x.toFixed(2)}" step="0.05" aria-label="X 坐标">m</label><label>Z <input id="posZ" type="number" value="${o.z.toFixed(2)}" step="0.05" aria-label="Z 坐标">m</label></div><div class="move-pad"><span></span><button data-nudge="0,-1" aria-label="向后移十厘米">↑</button><span></span><button data-nudge="-1,0" aria-label="向左移十厘米">←</button><span class="center" style="display:grid;place-items:center">10cm</span><button data-nudge="1,0" aria-label="向右移十厘米">→</button><span></span><button data-nudge="0,1" aria-label="向前移十厘米">↓</button></div>${collision.length?`<div class="overlap-note">占地可能重叠：${esc(collision.map(v=>v.name).join('、'))}。允许试摆，请检查通道与实物尺寸。</div>`:''}</div><div class="inspector-section"><div class="field-title">${icon('rotate')}旋转方向</div><div class="row"><button class="small-btn" id="rotateLeft" title="逆时针旋转 15 度">↶ 15°</button><span class="rotation-value">${((o.r*180/PI%360+360)%360).toFixed(0)}°</span><button class="small-btn" id="rotateRight" title="顺时针旋转 15 度">15° ↷</button></div></div>${portable?`<div class="inspector-section"><div class="field-title">${icon('plan')}摆放承载面</div><select class="select" id="surfaceSelect" aria-label="承载面"><option value="floor" ${!o.parent&&!o.surface?'selected':''}>地面</option><option value="bay" ${o.surface==='bay'?'selected':''} ${o.w>.48?'disabled':''}>东向飘窗</option>${Object.entries(supportY).filter(([k])=>!['sofa','pouf'].includes(k)).map(([k])=>`<option value="${k}" ${o.parent===k?'selected':''}>${renderer.map.get(k).name}</option>`).join('')}</select><p class="small-note">切换后自动放在对应表面；拖动不会改变高度。</p></div>`:''}${p?`<div class="inspector-section"><div class="field-title">${icon('leaf')}校正植物身份</div><select class="select" id="speciesSelect" aria-label="植物品种">${Object.entries(NestRoom.plants).map(([k,v])=>`<option value="${k}" ${o.species===k?'selected':''}>${v.name}</option>`).join('')}</select><p class="small-note">${p.note}</p></div>`:''}<div class="object-actions"><button class="small-btn" id="toggleVisible">${icon(o.visible?'eyeoff':'eye')}${o.visible?'暂时收起':'重新放回'}</button><button class="small-btn" id="resetObject">${icon('undo')}原位</button>${p?`<button class="small-btn full" id="duplicatePlant" style="grid-column:1/-1">${icon('copy')}复制这盆植物</button>`:''}</div>`;
 for(const axis of['X','Z'])$('#pos'+axis).onchange=e=>{let n=Number(e.target.value);if(!Number.isFinite(n)){renderInspector();return;}checkpoint();o[axis.toLowerCase()]=n;clamp(o);changed();};
 $$('#inspectorContent [data-nudge]').forEach(e=>e.onclick=()=>{const[x,z]=e.dataset.nudge.split(',').map(Number);nudge(x*.1,z*.1);});$('#rotateLeft').onclick=()=>rotate(-PI/12);$('#rotateRight').onclick=()=>rotate(PI/12);$('#toggleVisible').onclick=()=>{checkpoint();o.visible=!o.visible;changed();};$('#resetObject').onclick=()=>{const b=baseById.get(o.id);if(!b){toast('这是一盆复制的植物，没有照片原位。');return;}checkpoint();for(const k of['x','z','r','y','parent','surface'])o[k]=b[k];o.visible=true;changed();};
 if(portable)$('#surfaceSelect').onchange=e=>{checkpoint();const v=e.target.value;o.parent=Object.hasOwn(supportY,v)?v:null;o.surface=v==='bay'?'bay':null;o.y=o.parent?supportY[v]:v==='bay'?.74:0;o.x=v==='bay'?3.3:0;o.z=0;clamp(o);changed();};
 if(p){$('#speciesSelect').onchange=e=>{checkpoint();o.species=e.target.value;o.name=NestRoom.plants[o.species].name+' · 已手动校正';renderer.dispose(o.parts);o.parts=renderer.upload(NestRoom.model(o));changed();};$('#duplicatePlant').onclick=()=>{if(items.length>=120){toast('当前模型最多保留 120 件物品。');return;}checkpoint();const n={...o,id:'extra-'+Date.now().toString(36),name:o.name+' · 副本',pickId:Math.max(...items.map(i=>i.pickId))+1,x:o.x+.38,z:o.z+.18,parts:renderer.upload(NestRoom.model(o))};items.push(n);renderer.setScene(arch,items);clamp(n);select(n);changed();updateCount();toast('已复制，拖动给它找个位置。');};}}
function nudge(x,z){if(!selected)return;checkpoint();selected.x+=x;selected.z+=z;clamp(selected);changed();}
function rotate(angle){if(!selected)return;if(selected.wallMount){toast('墙饰朝向保持与背墙一致。');return;}checkpoint();selected.r+=angle;clamp(selected);changed();}
function refresh(){renderSidebar();renderInspector();updateHistory();updateCount();$('#currentLayout').textContent=NestRoom.presets.find(p=>p.id===presetId)?.name||'我的自由布置';draw();}
function updateCount(){$('#objectCount').textContent=`${items.length} 件可选物品 · ${items.filter(o=>o.category==='plants').length} 个植物 / 花材对象`;}
function modal(title,html){$('#dialogTitle').textContent=title;$('#dialogBody').innerHTML=html;$('#dialog').showModal();}
function help(){modal('欢迎回到你的小家',`<p>这是根据你提供的客厅照片制作的可编辑三维场景，不是整套住宅扫描，也不是施工图。</p><h3>怎么布置</h3><p>直接拖动物品，在 X-Z 水平面移动；高度固定。拖动空白处环绕视角，滚轮缩放。按住空格或鼠标右键拖动可平移视角。手机单指拖物、空白处环绕，双指缩放。</p><p>选中后 <kbd>R</kbd> 旋转 15°，<kbd>Shift R</kbd> 反向旋转；方向键移动 10 cm，按住 Shift 为 1 cm；<kbd>Delete</kbd> 收起；<kbd>Esc</kbd> 取消选择；<kbd>Ctrl / ⌘ Z</kbd> 撤销，<kbd>Ctrl / ⌘ Shift Z</kbd> 重做。</p><h3>哪些能调整</h3><p>家具、盆栽、抱枕、屏幕、键盘、茶具组、墙饰与吊灯可独立选择。家具移动会带着其表面物件。茶具以整组建模，未把照片里每件极小摆件单独精确复刻。墙窗和飘窗结构固定，窗帘与吊灯显示可在场景设置调整。</p><h3>关于尺寸与光照</h3><p>可见客厅暂按 <b>7.2 × 5.8 × 2.85 m</b> 估算；家具尺寸、窗洞与门位依据照片透视重建，未经测量。晨光、日间、暖夜为视觉氛围，不计算城市、日期、楼间遮挡或真实照度。</p><h3>植物识别与资料</h3><p>大型蕉形叶疑似鹤望兰；裂叶大叶较像龟背竹；细叶黄绿植疑似蕨类。其余线叶植物、绣球样花材、小盆均标注待确认。真假植物和切花状态无法只凭这些照片确定。可在面板手动校正。</p><p>养护背景参考：<a href="https://plants.ces.ncsu.edu/plants/strelitzia-nicolai/" target="_blank" rel="noopener noreferrer">NC State · 鹤望兰</a>、<a href="https://plants.ces.ncsu.edu/plants/monstera-deliciosa/" target="_blank" rel="noopener noreferrer">龟背竹</a>、<a href="https://plants.ces.ncsu.edu/plants/nephrolepis-exaltata/" target="_blank" rel="noopener noreferrer">肾蕨</a>。这些资料不代替品种确认。</p><h3>保存与隐私</h3><p>自动保存与收藏都只存于当前浏览器，不上传云端，不跨设备同步。清除浏览器数据会删除它们；请导出 JSON 备份。网页不包含原始室内照片，也不使用外部模型、字体或分析追踪。</p>`);}
function saveSlots(){let slots;try{slots=JSON.parse(localStorage.getItem(SLOTS)||'[]');if(!Array.isArray(slots))slots=[];}catch{slots=[];}modal('收藏喜欢的摆法',`<p>给今天的灵感留一个位置。收藏只保存在这个浏览器；跨设备请导出布局。</p>${[0,1,2].map(i=>`<div class="slot"><div><strong>${esc(slots[i]?.name||'空白收藏 '+(i+1))}</strong><small>${slots[i]?esc(slots[i].date):'还没有收藏方案'}</small></div><button class="small-btn" data-load-slot="${i}" ${slots[i]?'':'disabled'}>打开</button><button class="small-btn" data-save-slot="${i}">存入</button></div>`).join('')}<div class="dialog-actions"><button class="button subtle" id="slotExport">导出 JSON 备份</button></div>`);$$('[data-save-slot]').forEach(b=>b.onclick=()=>{const i=+b.dataset.saveSlot;const name=window.prompt('给方案取个名字',slots[i]?.name||'我的东窗小家');if(name===null)return;slots[i]={name:(name.trim()||'我的东窗小家').slice(0,40),date:new Date().toLocaleString('zh-CN'),state:snapshot()};try{localStorage.setItem(SLOTS,JSON.stringify(slots));toast('已收藏到此浏览器。');$('#dialog').close();}catch{toast('浏览器不允许保存，请导出 JSON 备份。');}});$$('[data-load-slot]').forEach(b=>b.onclick=()=>{try{checkpoint();restore(slots[+b.dataset.loadSlot].state);scheduleSave();$('#dialog').close();toast('已打开收藏方案。');}catch(e){toast(e.message);}});$('#slotExport').onclick=exportLayout;}
function download(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1500);}
function exportLayout(){download(new Blob([JSON.stringify(snapshot(),null,2)],{type:'application/json'}),'东窗小筑-布局.json');toast('布局已导出。导入同一文件可恢复。');}
function capture(){const keep=selected;selected=null;updateRing();renderer.frame(false);const source=$('#roomCanvas'),c=document.createElement('canvas');c.width=source.width;c.height=source.height+72;const ctx=c.getContext('2d');ctx.fillStyle='#f6f2e7';ctx.fillRect(0,0,c.width,c.height);ctx.drawImage(source,0,0);ctx.fillStyle='#587150';ctx.font='20px serif';ctx.fillText('东窗小筑 · 我的客厅',26,c.height-40);ctx.fillStyle='#9a9e86';ctx.font='11px sans-serif';ctx.fillText('照片估算重建 · 布局灵感，不作施工与日照依据',26,c.height-20);c.toBlob(blob=>{if(blob)download(blob,'东窗小筑-我的客厅.png');},'image/png');selected=keep;draw(false);toast('已生成房间照片。');}
function options(){modal('小世界的显示方式',`<label class="option-row">显示墙面与窗帘<input type="checkbox" id="wallOption" ${renderer.walls?'checked':''}></label><label class="option-row">显示吊灯（默认隐藏以便布置）<input type="checkbox" id="ceilingOption" ${renderer.ceiling?'checked':''}></label><label class="option-row">显示植物名字<input type="checkbox" id="labelOption" ${labels?'checked':''}></label><label class="option-row">合上窗帘<input type="checkbox" id="curtainOption" ${closedCurtains?'checked':''}></label><p class="small-note">俯视模式自动隐藏高墙、窗帘，方便准确拖动。窗帘状态只调整外观，不进行真实光照计算。</p><div class="dialog-actions"><button class="button primary" id="optionsDone">回到小家</button></div>`);$('#wallOption').onchange=e=>{renderer.walls=e.target.checked;draw();};$('#ceilingOption').onchange=e=>{renderer.ceiling=e.target.checked;draw();};$('#labelOption').onchange=e=>{labels=e.target.checked;draw(false);};$('#curtainOption').onchange=e=>{checkpoint();closedCurtains=e.target.checked;buildArch();scheduleSave();draw();};$('#optionsDone').onclick=()=>$('#dialog').close();}
function localPoint(e){const rect=$('#roomCanvas').getBoundingClientRect();return[e.clientX-rect.left,e.clientY-rect.top];}
function pointerDown(e){if($('#dialog').open)return;const canvas=$('#roomCanvas'),[x,y]=localPoint(e);pointers.set(e.pointerId,[x,y]);canvas.setPointerCapture(e.pointerId);
 if(pointers.size===2){if(drag?.object&&drag.changed){drag.object.x=drag.startX;drag.object.z=drag.startZ;if(drag.checkpointed){undo.pop();updateHistory();}draw();}drag=null;const p=[...pointers.values()];pinch={distance:Math.hypot(p[0][0]-p[1][0],p[0][1]-p[1][1]),zoom:renderer.zoom};return;}
 if(pointers.size>2)return;e.preventDefault();const hit=mode==='move'&&!space&&e.button===0?renderer.pick(x,y):null;
 if(hit){select(hit);const w=world(hit),plane=renderer.point(x,y,w[1]);drag={object:hit,id:e.pointerId,start:[x,y],startX:hit.x,startZ:hit.z,planeY:w[1],offset:[w[0]-plane[0],w[2]-plane[2]],changed:false};canvas.style.cursor='grabbing';}
 else drag={camera:true,id:e.pointerId,start:[x,y],last:[x,y],pan:space||e.button===2||e.button===1,moved:false};}
function pointerMove(e){if(!pointers.has(e.pointerId))return;const [x,y]=localPoint(e);pointers.set(e.pointerId,[x,y]);if(pinch&&pointers.size>=2){const p=[...pointers.values()],d=Math.hypot(p[0][0]-p[1][0],p[0][1]-p[1][1]);renderer.zoom=Math.max(.55,Math.min(2.8,pinch.zoom*d/Math.max(10,pinch.distance)));draw(false);return;}if(!drag||drag.id!==e.pointerId)return;e.preventDefault();
 if(drag.camera){let dx=x-drag.last[0],dy=y-drag.last[1];if(Math.hypot(x-drag.start[0],y-drag.start[1])>4)drag.moved=true;if(drag.pan){const scale=renderer.half*2/renderer.height;renderer.target[0]-=(renderer.right[0]*dx-renderer.up[0]*dy)*scale;renderer.target[2]-=(renderer.right[2]*dx-renderer.up[2]*dy)*scale;renderer.target[0]=Math.max(-3,Math.min(3,renderer.target[0]));renderer.target[2]=Math.max(-3,Math.min(3,renderer.target[2]));}else if(!renderer.top){renderer.azimuth-=dx*.006;renderer.elevation=Math.max(.28,Math.min(1.30,renderer.elevation+dy*.005));}drag.last=[x,y];draw(false);return;}
 if(Math.hypot(x-drag.start[0],y-drag.start[1])<4&&!drag.changed)return;if(!drag.checkpointed){checkpoint();drag.checkpointed=true;}const o=drag.object,p=renderer.point(x,y,drag.planeY);let xx=p[0]+drag.offset[0],zz=p[2]+drag.offset[1];if(o.parent){const parent=renderer.map.get(o.parent),wp=world(parent),r=parent.r,dx=xx-wp[0],dz=zz-wp[2];xx=dx*Math.cos(r)-dz*Math.sin(r);zz=dx*Math.sin(r)+dz*Math.cos(r);}if($('#snapToggle').checked){xx=Math.round(xx*10)/10;zz=Math.round(zz*10)/10;}o.x=xx;o.z=zz;clamp(o);drag.changed=true;draw();if($('#posX'))$('#posX').value=o.x.toFixed(2);if($('#posZ'))$('#posZ').value=o.z.toFixed(2);}
function pointerUp(e){pointers.delete(e.pointerId);if(pinch){if(pointers.size<2)pinch=null;drag=null;$('#roomCanvas').style.cursor='grab';return;}if(drag?.id===e.pointerId){if(drag.object&&drag.changed)changed();if(drag.camera&&!drag.moved&&e.button===0)select(null);drag=null;}$('#roomCanvas').style.cursor='grab';}
function bind(){const c=$('#roomCanvas');c.addEventListener('pointerdown',pointerDown);c.addEventListener('pointermove',pointerMove);c.addEventListener('pointerup',pointerUp);c.addEventListener('pointercancel',pointerUp);c.addEventListener('contextmenu',e=>e.preventDefault());c.addEventListener('wheel',e=>{e.preventDefault();renderer.zoom=Math.max(.55,Math.min(2.8,renderer.zoom*Math.exp(-e.deltaY*.001)));draw(false);},{passive:false});
 $$('.tab').forEach(b=>b.onclick=()=>{tab=b.dataset.tab;$$('.tab').forEach(t=>{t.classList.toggle('active',t===b);t.setAttribute('aria-selected',t===b?'true':'false');});renderSidebar();});
 $('#moveMode').onclick=()=>setMode('move');$('#orbitMode').onclick=()=>setMode('orbit');$('#zoomIn').onclick=()=>{renderer.zoom=Math.min(2.8,renderer.zoom*1.16);draw(false);};$('#zoomOut').onclick=()=>{renderer.zoom=Math.max(.55,renderer.zoom/1.16);draw(false);};$('#resetView').onclick=()=>{renderer.azimuth=-.60;renderer.elevation=.72;renderer.target=[0,.65,0];renderer.zoom=1;setView(false);};$('#isoView').onclick=()=>setView(false);$('#topView').onclick=()=>setView(true);
 $$('[data-light]').forEach(b=>b.onclick=()=>{let v=b.dataset.light;renderer.night=v==='night'?1:v==='morning'?.10:0;$$('[data-light]').forEach(e=>e.classList.toggle('active',e===b));draw(false);});
 $('#undoBtn').onclick=()=>{if(!undo.length)return;redo.push(snapshot());const st=undo.pop();restore(st);scheduleSave();toast('已撤销上一步。');};$('#redoBtn').onclick=()=>{if(!redo.length)return;undo.push(snapshot());const st=redo.pop();restore(st);scheduleSave();toast('已重做。');};$('#closeInspector').onclick=()=>select(null);$('#optionsBtn').onclick=options;$('#helpBtn').onclick=help;$('#infoBtn').onclick=help;$('#saveBtn').onclick=saveSlots;$('#captureBtn').onclick=capture;$('#exportBtn').onclick=exportLayout;$('#importBtn').onclick=()=>$('#fileInput').click();$('#dialogClose').onclick=()=>$('#dialog').close();$('#dialog').onclick=e=>{if(e.target===$('#dialog')){const r=$('#dialog').getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)$('#dialog').close();}};$('#mobileLibrary').onclick=()=>{$('#sidebar').classList.toggle('open');select(null);};
 $('#fileInput').onchange=async e=>{const f=e.target.files[0];if(!f)return;if(f.size>250000){toast('文件过大，请选择导出的布局 JSON。');e.target.value='';return;}const previous=snapshot();try{const d=JSON.parse(await f.text());checkpoint();restore(d,true);scheduleSave();}catch(error){try{restore(previous);}catch{}if(undo.length)undo.pop();updateHistory();toast(error.message||'导入失败，原布局未改变。');}finally{e.target.value='';}};
 document.addEventListener('keydown',e=>{if(['INPUT','SELECT','TEXTAREA'].includes(document.activeElement.tagName)||$('#dialog').open)return;const modifier=e.ctrlKey||e.metaKey;if(modifier&&e.key.toLowerCase()==='z'){e.preventDefault();(e.shiftKey?$('#redoBtn'):$('#undoBtn')).click();return;}if(modifier&&e.key.toLowerCase()==='y'){e.preventDefault();$('#redoBtn').click();return;}if(e.code==='Space'){e.preventDefault();space=true;}if(e.key==='Escape'){select(null);$('#sidebar').classList.remove('open');}if(e.key.toLowerCase()==='r'&&selected){e.preventDefault();rotate((e.shiftKey?-1:1)*PI/12);}if(e.key==='Delete'&&selected){e.preventDefault();checkpoint();selected.visible=false;changed();}const dirs={ArrowLeft:[-1,0],ArrowRight:[1,0],ArrowUp:[0,-1],ArrowDown:[0,1]};if(selected&&dirs[e.key]){e.preventDefault();const d=dirs[e.key],s=e.shiftKey?.01:.1;nudge(d[0]*s,d[1]*s);}});document.addEventListener('keyup',e=>{if(e.code==='Space')space=false;});window.addEventListener('blur',()=>{space=false;if(drag?.changed)changed();drag=null;pointers.clear();pinch=null;});window.addEventListener('resize',()=>{renderer.resize();draw(false);});window.addEventListener('beforeunload',()=>{try{localStorage.setItem(KEY,JSON.stringify(snapshot()));}catch{}});
}
function setMode(v){mode=v;$('#moveMode').classList.toggle('active',v==='move');$('#orbitMode').classList.toggle('active',v==='orbit');toast(v==='move'?'布置模式：拖动物品，只在平面移动。':'环绕模式：拖动任意位置旋转视角。');}
function setView(top){renderer.top=top;if(top){renderer.azimuth=0;renderer.target=[0,0,0];}else{renderer.azimuth=-.60;renderer.target=[0,.65,0];}$('#isoView').classList.toggle('active',!top);$('#topView').classList.toggle('active',top);draw();}
function boot(){try{const textures=NestRoom.textures();try{renderer=new Nest3D.Renderer($('#roomCanvas'),textures);}catch(gpuError){const old=$('#roomCanvas'),fresh=old.cloneNode(false);old.replaceWith(fresh);renderer=new Nest3D.CanvasRenderer(fresh,textures);console.info('Using compatible canvas 3D renderer:',gpuError.message);}items=copyBase();items.forEach((o,i)=>{o.pickId=i+1;o.parts=renderer.upload(NestRoom.model(o));});buildArch();renderer.setScene(arch,items);renderer.night=.10;bind();let saved=null;try{saved=JSON.parse(localStorage.getItem(KEY)||'null');}catch{storageOK=false;}if(saved){try{restore(saved);}catch{refresh();toast('旧存档无法读取，已打开照片原位。');}}else refresh();if(!storageOK)$('#saveStatus').textContent='保存不可用 · 请导出布局';updateHistory();renderer.frame();$('#loading').classList.add('done');setTimeout(()=>$('#loading').hidden=true,650);
 // Small, read-only inspection hooks used for UI regression testing and reproducible diagnostics.
 window.EastWindow={getState:()=>JSON.parse(JSON.stringify(snapshot())),getView:()=>({top:renderer.top,zoom:renderer.zoom,azimuth:renderer.azimuth}),project:id=>{const o=renderer.map.get(id);return o?renderer.project(world(o)):null;},getSelected:()=>selected?.id||null,stats:()=>({objects:items.length,vertices:items.reduce((s,o)=>s+o.parts.reduce((n,p)=>n+p.count,0),0),shadow:!!renderer.shadowExt,engine:renderer.engine||'webgl'})};
 }catch(e){$('#loading').innerHTML=`<strong>小世界暂时没能打开</strong><small>${esc(e.message)}</small><p style="padding:20px;text-align:center">请尝试支持 WebGL、已开启硬件加速的现代浏览器。<br>桌面版 Chrome、Edge、Safari 或 Firefox 均可尝试。</p>`;console.error(e);}}
setTimeout(boot,40);
})();
