# 东窗小筑 · East Window Studio

根据客厅照片制作的温馨三维布局小游戏。只重建照片中可见的客厅，不包含原始照片。

在线页面：https://tbtnb.github.io/yuanzhuang-editable-3d/east-window/

## v2：按用户标注修订

- 只向截图下方增加 1.6 m 进深，原家具、工作区、门和东向飘窗不拉伸。此数值只是估算；场景设置可把增加进深调整为 0.5–4 m。初始总进深约 7.4 m。
- 窗边设备纠正为投影仪，增加镜头、指示灯、散热格栅与独立落地支架；保留历史 ID `speaker` 以兼容旧存档，不再把它称作音箱或灯架。
- 新前区增加朝向沙发的电动投影幕布，支持展开、收起、示意投影、平面拖动及一键朝向。立体背面和俯视自动剖切幕面以免遮挡布置；“看幕布”切到正面。
- 九个窗台盆栽使用八种外形：分枝小花、心叶垂蔓、披针叶、红色宽瓣花材、圆叶、浅斑纹长叶、羽状复叶、小叶灌丛；第九盆以更高、更密的披针叶建模。这些是照片外形近似，不是已确认的品种。
- “幕布电影夜”的沙发朝向前区幕布，不再转向背后的电脑大屏。
- v1 自动保存、收藏槽和 JSON 在读入时迁移。保留用户家具坐标，补齐幕布、纠正旧设备名与默认小盆模型；已手动校正的植物身份保留。

## 操作

直接拖动物品进行 X-Z 平面移动，高度锁定。拖动空白处环绕，滚轮缩放；空格或鼠标右键拖动平移视角。手机单指拖动物品，双指缩放。俯视模式便于精准摆放。

选中后 R 旋转 15°，Shift+R 反转。方向键移动 10 cm，Shift+方向键移动 1 cm。Delete 收起，Esc 取消选择，Ctrl/Command+Z 撤销，Ctrl/Command+Shift+Z 重做。

44 个可选对象，含 16 个植物/花材对象。家具表面的物件随承载家具移动，也可独立调整。植物和部分摆件可切换到地面、飘窗、桌面或柜面；拖动不改变高度。提供照片原位、追光小森林、幕布电影夜、清爽留白四套预设，晨光/日间/暖夜三种视觉氛围，以及重叠提醒、植物品种校正和复制。

自动保存与三个收藏槽只存在当前浏览器，不跨设备同步。导出 JSON 可备份布局，导入可恢复；拍照按钮导出 PNG。

## 重建边界

房间暂按 7.2 × 7.4 × 2.85 m 估算，家具尺寸和门窗位置不是实测数据。墙窗与飘窗结构固定。茶具按组建模，未精确复刻每个微小摆件。窗外楼体为泛化示意，不复原住址。

植物识别基于照片外形，包含置信度与人工校正；小盆、花材和真假植物状态保持待确认。投影画面不模拟真实投射比、侧投校正、光路或物体遮挡；幕布与吊装尺寸仍须实测。灯光只表现氛围，不做真实照度、日照时间或楼体遮挡计算，不可作为养护和施工的定量依据。植物背景资料链接见网页使用说明。

## 本地运行与文件

无构建步骤、无第三方依赖，打开 index.html 即可；也可以在本目录执行 `python -m http.server 8000` 后访问本地服务器。

- `engine.js`：WebGL 几何、阴影、相机及物品拾取。
- `compat.js`：无 WebGL 时使用的 Canvas 软件三维渲染器。
- `room.js`：程序化模型、纹理、植物图鉴和预设坐标。
- `revision.js`：标注修订后的前区、投影系统、窗台形态模型与旧存档迁移。
- `app.js`：拖放、承载面、历史、存档、导入导出和界面逻辑。
- `styles.css`、`index.html`：响应式界面。

所有纹理和模型本地生成，不请求外部资源、字体或分析追踪。浏览器禁止本地存储时请导出布局文件。

## 测试范围

使用 Chromium 自动化检查了桌面 1512×982 与手机 393×852 布局，四个预设、平面拖动、子物件跟随、撤销重做、旋转、植物承载面与品种切换、复制、边界约束、JSON 导出/导入及错误恢复、收藏与 PNG 导出；手机另测单指拖动和双指缩放。

v2 另通过 34 项回归检查：前区可用边界、幕布实际拖动、卷起几何变化、投影开关、镜头朝向、四个预设、进深编辑与撤销、植物跨承载面移动、导入导出、无损错误恢复、v1 存档迁移，以及手机投影与植物面板。测试环境无 WebGL，实际画面测试使用 Canvas 兼容路径；WebGL 硬件路径未经该环境实机验证。自动保存逻辑在测试用存储适配器中验证，真实浏览器的存储策略仍由使用者环境决定。
